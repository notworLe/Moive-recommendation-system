# Run server
`fastapi dev <file>`
Example: 
- `fastapi dev main.py`
 
`uvicorn main:app --reload`

# Docker
## Run MySQL
`docker exec -it my-mysql mysql -u root -p`


# Requirement
pip freeze


pip install -r requirements.txt

# Venv
python -m venv venv
